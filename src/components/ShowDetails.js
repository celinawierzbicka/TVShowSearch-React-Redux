import React from 'react';
import { Link } from 'react-router-dom';

const ShowDetails = function(props) {
        return (
        <div className="list">
                <Link to="/"><button>aaa</button>Back to search results</Link>
        </div>
    );
};

export default ShowDetails;
